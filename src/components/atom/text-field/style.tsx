"use client";

import styled from "@emotion/styled";
import { MyTextFieldProps } from "./interface";
import { TextField } from "@mui/material";

export const TextFieldContainer = styled(TextField)<MyTextFieldProps>`
`;
